<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools">

  <data>

    <variable
      name="viewmodel"
      type="com.logic.jellyfish.ui.login.register.RegisterViewModel" />
  </data>

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/login_background"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    tools:context=".ui.login.login.LoginActivity">

    <ImageView
      android:layout_width="@dimen/login_ic"
      android:layout_height="@dimen/login_ic"
      android:layout_marginTop="32dp"
      app:srcCompat="@mipmap/logo" />

    <EditText
      android:layout_width="@dimen/login_edit_text_width"
      android:layout_height="wrap_content"
      android:layout_marginTop="24dp"
      android:background="@drawable/btn_rounded_translucent"
      android:hint="请输入手机号码"
      android:paddingLeft="20dp"
      android:text="@={viewmodel.phoneNumber}" />

    <EditText
      android:layout_width="@dimen/login_edit_text_width"
      android:layout_height="wrap_content"
      android:layout_marginTop="24dp"
      android:background="@drawable/btn_rounded_translucent"
      android:hint="请输入验证码"
      android:paddingLeft="20dp"
      android:text="@={viewmodel.verificationCode}" />

    <LinearLayout
      android:layout_width="@dimen/login_edit_text_width"
      android:layout_height="wrap_content"
      android:layout_marginTop="24dp"
      android:background="@drawable/btn_rounded_translucent"
      android:gravity="center_vertical"
      android:orientation="horizontal">

      <EditText
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:background="@color/transparent"
        android:hint="请输入登陆密码"
        android:inputType="textPassword"
        android:paddingLeft="20dp"
        android:text="@={viewmodel.password}" />

      <!--<ImageButton-->
      <!--android:layout_width="50dp"-->
      <!--android:layout_height="match_parent"-->
      <!--android:layout_marginRight="30dp"-->
      <!--android:adjustViewBounds="true"-->
      <!--android:background="@color/transparent"-->
      <!--app:srcCompat="@mipmap/mm_icon" />-->

    </LinearLayout>

    <LinearLayout
      android:layout_width="@dimen/login_edit_text_width"
      android:layout_height="wrap_content"
      android:layout_marginTop="24dp"
      android:background="@drawable/btn_rounded_translucent"
      android:gravity="center_vertical"
      android:orientation="horizontal">

      <EditText
        android:layout_width="@dimen/login_edit_text_width"
        android:layout_height="wrap_content"
        android:background="@color/transparent"
        android:hint="请再次确认密码"
        android:inputType="textPassword"
        android:paddingLeft="20dp"
        android:text="@={viewmodel.confirmPassword}" />
    </LinearLayout>

    <LinearLayout
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginTop="12dp"
      android:gravity="center_vertical"
      android:orientation="horizontal">

      <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:buttonTint="@color/white"
        android:checked="@={viewmodel.agreement}"
        android:text="已阅读并同意"
        android:textColor="@color/white"
        android:textSize="14sp" />

      <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{(v) -> viewmodel.navigateToAgreement(v)}"
        android:text="《用户服务协议》"
        android:textColor="@color/colorPrimary"
        android:textSize="16sp" />

    </LinearLayout>

    <Button
      android:layout_width="@dimen/login_btn_width"
      android:layout_height="wrap_content"
      android:layout_marginTop="12dp"
      android:background="@drawable/btn_rounded_green_300"
      android:onClick="@{(v) -> viewmodel.register(v)}"
      android:text="注册"
      android:textColor="@color/white" />


    <TextView
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_alignParentStart="true"
      android:layout_marginStart="0dp"
      android:layout_marginTop="20dp"
      android:onClick="@{(v) -> viewmodel.navigateToLogin(v)}"
      android:text="已有账号，立即登录"
      android:textColor="@color/white" />


  </LinearLayout>

</layout>